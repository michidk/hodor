services:
  gate:
    build: .
    ports:
      - "8080:8080"
    environment:
      PASSWORD: "changeme"
      UPSTREAM: "http://app:80"
    depends_on:
      - app

  app:
    image: traefik/whoami
